# Deployment

The maintained installation, server migration, daily operation, self-update,
verification, and troubleshooting instructions live in the repository
[`README.md`](../README.md).

That guide is the source of truth. Older version-specific documents under
`docs/` are retained only as historical records and must not be used to install
the current `continuous` release.
